# DAS Bullets — Unverified Draft (Claude-authored, NOT fact-checked)

> **STATUS: DO NOT USE AS SOURCE OF TRUTH.**
> These bullets were generated by a different model against a research report on 2026 engineering
> JDs. They were reviewed against this project's `FACT_RULES.md` and found to violate it:
>
> - Every Full Stack and Backend bullet claims "Built," "Implemented," or "Set up" for the Rainbow
>   City MMS, which is confirmed design-only (no source code, no deployment, per two independent
>   audits run by the user against the project's own documents).
> - Several bullets name technology never confirmed in the DAS stack (Go, RabbitMQ, LangChain) or
>   pulled from a *different* project entirely (Redis is from Hearloop, NumPy is from Fake Review
>   Detector — cross-project contamination, not DAS facts).
> - AI Engineer bullets 3 and 4 describe a "shadow-mode duplicate-triage agentic system" and a
>   "cited runbook assistant using RAG" as already built. The source report explicitly labels both
>   as **"AI sidecar — not MVP"** — hypothetical future proposals, not shipped work.
>
> Stored here at the user's explicit request for reference during future tailoring, **not** as
> confirmed content. Any bullet pulled from this file for an actual resume must first pass the
> same fact-check process used for `career-stories/eInfochips.md` and the confirmed DAS bullets in
> `resume-system/facts/work-experience.md` — source every claim, correct the verb tense to match actual build
> status, remove or replace any unconfirmed/cross-project tech, and label every metric MEASURED/
> ESTIMATE or flag it for the metric-legibility check per `resume-system/governance/FACT_RULES.md`.

---

## Full-Stack Engineer (unverified)

- Built a membership system using React, TypeScript, HTML, and CSS for a non-profit, replacing scattered forms and spreadsheets with one guided path from interest to active member and cutting the time staff spent chasing duplicate records.
- Implemented a role-scoped member workspace with SQL-based row-level security and a Node.js backend, delivered through Agile sprints, so managers only saw their own group's data and staff no longer needed to manually check permissions.
- Built a payment and reconciliation interface using REST APIs on Cloud infrastructure with DevOps-managed deployments, turning payment mismatches into visible exceptions so staff caught problems before they became member complaints.
- Set up CI/CD pipelines and Git-based version control alongside JavaScript tooling across the app, letting the client deploy and monitor the system on their own and removing their dependency on one developer to keep things running.

## Backend Engineer (unverified)

- Designed the member lifecycle domain model with SQL-backed status transitions exposed through REST APIs, versioned in Git, so staff could trust the roster's active and overdue statuses without manually verifying them.
- Built a Node.js service to process payment and intake-form webhooks, containerized with Docker and deployed through CI/CD, preventing duplicate records and lost signups under real-world failures.
- Built a read-only reconciliation engine using Redis for fast lookups and running on Cloud infrastructure, delivered through Agile sprints, cutting the manual cross-checking staff previously did by hand.
- Hardened the backend in Go with RabbitMQ-driven audit event messaging, letting the client safely operate and review system activity without depending on one developer.

## AI Engineer (unverified)

- Built a data layer over the organization's operational records that tracked where each field came from using SQL and Cloud infrastructure, version-controlled in Git as part of a production system, giving downstream automation trustworthy inputs instead of guesswork.
- Built a golden evaluation dataset and benchmark for duplicate detection and payment matching, applying MLOps and AI architecture practices with NumPy-based scoring, preventing costly false matches before they reached production.
- Built a shadow-mode duplicate-triage agentic system powered by an LLM that surfaced candidate matches with evidence and confidence scores, communicating results clearly to non-technical reviewers who kept the final merge decision.
- Built a cited runbook assistant using RAG and LangChain with AI orchestration over internal documentation, written in Python, cutting the time operators spent digging through scattered docs to resolve incidents.

---

## Why these are flagged, bullet by bullet

| Bullet | Issue |
|---|---|
| FS #2 | Names "Node.js backend" — DAS confirmed stack is TypeScript Azure Functions, not Node.js as a framework choice |
| FS #3, BE #2, BE #3 | "Cloud infrastructure" / "REST APIs on Cloud" — generic where confirmed facts are specific (Azure, Postgres work ledger vs. Service Bus tradeoff) |
| BE #2 | "Docker" — not in confirmed DAS stack |
| BE #3 | "Redis" — belongs to Hearloop, not DAS |
| BE #4 | "Go," "RabbitMQ" — appear nowhere in any confirmed project or work-experience fact |
| AI #2 | "NumPy-based scoring" — belongs to Fake Review Detector, not DAS |
| AI #4 | "RAG and LangChain... written in Python" — belongs to Video Compliance project, not DAS |
| AI #3, AI #4 | Both describe source report's explicitly-labeled "AI sidecar — not MVP" proposals as completed, shipped work |
| All FS/BE, most AI | Verb tense ("Built," "Implemented," "Set up," "Hardened") contradicts confirmed status: design phase, no code, no deployment |

## Source

Pasted into conversation by the user, attributed to a separate AI assistant session, alongside a
research report ("2026 engineering-role job-description research") comparing Full-Stack/Backend/
AI Engineer postings against the Rainbow City MMS architecture. The research report itself is
solid and well-sourced; the bullet-locking step at the end of that report did not apply its own
constraints or this project's fact-check discipline.
